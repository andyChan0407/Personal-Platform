# PRD · 个人专属待办桌面应用（第一版）

> 状态：需求与界面设计已确认，作为后续开发依据。
> 文档创建：2026-09-14
> 形态：Windows 桌面程序（双击打开，不登录、不联网、不连云，数据存本机）。

---

## 1. 产品目标与使用场景

### 1.1 产品目标
做一个**只给本人使用**的轻量桌面工具，把「记待办、专注计时、随手计时、按日期看安排」四件事合在一个离线、隐私、零配置的本地应用里。无账号、无云端、无协作，打开即用。

### 1.2 使用场景
- 坐在电脑前，随手记录今天 / 这段时间要做的几件事。
- 用番茄钟进入专注状态（25 分钟工作 + 5 分钟休息循环），阶段切换有声音和弹窗提醒。
- 用秒表记录某件事花了多少时间（如「读了 42 分钟书」），可分段计次。
- 在日历上按日期查看每天有哪些待办，回顾当天安排。
- 全程离线、断网可用，数据只在本机。

---

## 2. 整体页面框架与导航结构

- **单窗口桌面应用**：窗口内分两块——
  - **左侧固定导航栏**：4 个入口 + 底部「计时状态条」。
  - **右侧主内容区**：随导航选中项切换显示对应模块。
- **导航项（顺序）**：
  1. 待办清单（首页 / 默认选中）
  2. 番茄计时
  3. 计时器
  4. 日历
- **导航栏底部计时状态条**：番茄钟或秒表运行中时，显示当前时间 + 阶段（如「番茄 24:12 工作中」），点击可跳到对应模块；无运行时不显示或显示空闲。
- **视觉规范**：浅色主题、扁平（无渐变 / 阴影）；主色蓝 `#185FA5`，番茄红作强调；界面参照已确认的 4 张原型（待办清单 / 番茄计时 / 秒表 / 日历）。

---

## 3. 首页内容结构

首页 = **待办清单**，打开应用第一眼看到的就是它。从上到下：

1. **顶部操作条**：标题「待办清单」+ 搜索框 + 「+ 新建待办」按钮。
2. **筛选标签**：全部 / 未完成 / 已完成（默认「全部」）。
3. **待办列表**：每条一行 = 复选框 + 标题 + 日期徽标（有日期才显示，「未排期」无日期项显示「未排期」）。完成的项标题加删除线、置灰。
4. **空状态**：列表为空时显示一句引导文案（如「还没有待办，点右上角新建一个」）。

新建 / 编辑待办用**小弹窗**：标题（必填）、日期（可选）、备注（可选）。

---

## 4. 各模块用途与第一版最必要的数据和操作

### 4.1 待办清单（首页）
- **用途**：录入、编辑、管理待办；标记完成；安排日期。
- **每条待办最必要的数据**：
  - `id`：唯一标识
  - `title`：标题（必填）
  - `date`：日期（可选，格式 `YYYY-MM-DD`；空 = 未排期）
  - `done`：是否完成（true / false）
  - `note`：备注（可选）
  - `createdAt`：创建时间
- **最必要操作**：新增（弹窗）、编辑、删除、勾选完成 / 取消、按「全部 / 未完成 / 已完成」筛选、按标题搜索。

### 4.2 番茄计时
- **用途**：番茄工作法专注计时，工作 / 休息自动循环；阶段切换用「声音 + 桌面弹窗」提醒。
- **最必要数据（运行态）**：
  - 工作时长（默认 25 分，可改）、休息时长（默认 5 分，可改）
  - 当前阶段（工作 / 休息）、剩余秒数、已完成番茄计数、运行状态（运行 / 暂停）
  - 注：运行态可不持久化，关程序即重置，无需存档。
- **最必要操作**：开始、暂停 / 继续、跳过（切到下一阶段）、重置、调整时长（改后生效）。

### 4.3 计时器（秒表）
- **用途**：正计时，记录过去了多久；可分段计次。
- **最必要数据（运行态）**：累计毫秒、运行状态、计次列表（每段时长）。
- **最必要操作**：开始、暂停 / 继续、计次、清零（清零后归零并清空计次）。

### 4.4 日历
- **用途**：月视图浏览日期，按日期查看当天待办。
- **最必要数据**：当前查看的年 / 月、选中日期、该日待办（由待办清单按 `date` 聚合，非独立存储）。
- **最必要操作**：上一月 / 下一月、回到「今天」、点击某天查看当天待办、在日历内勾选完成待办。

---

## 5. 待办清单与日历模块之间的关系

- **同一份数据，两种视图**：待办清单是数据的「录入与编辑」主入口；日历是同一份待办按 `date` 字段的「时间视图」。两者共享同一个本地数据文件，不各自存储。
- **未排期待办**：`date` 为空的待办归入「未排期」，不出现在日历的具体某天，仅在待办清单的「未排期」中可见。
- **联动**：在待办清单新建 / 编辑带日期的待办 → 自动出现在日历对应日期格子（显示数量圆点）；在日历点击某天 → 右侧列出该日待办并可勾选完成，**与待办清单双向同步**（因为是同一数据源）。
- **删除 / 编辑**：任一模块删除或编辑待办，另一模块同步更新。

---

## 6. 本地数据文件、备份与恢复要求

- **存储位置**：所有数据存于本机**一个本地文件**（如 JSON，置于程序目录或用户文档目录下的 `data.json` 之类）。不联网、不上云、不跨设备。
- **读写时机**：程序启动时读取该文件；任意增删改后自动保存（或定时保存）。
- **备份（第一版）**：不做自动导出 / 导入功能。因数据集中在单一文件，用户可**手动复制该文件**完成备份。
- **恢复**：用备份文件覆盖原数据文件后，重启程序即恢复。
- **容错**：数据文件损坏或丢失时，程序应能以**空数据正常启动、不崩溃**（恢复为空状态，并可在下次写入时重建文件）。

---

## 7. 第一版必须完成的功能

- [ ] Windows 桌面程序：双击打开，离线可用，数据存本机文件。
- [ ] 左侧导航 + 右侧主区，4 个模块切换；首页默认待办清单。
- [ ] 待办清单：新增 / 编辑 / 删除、勾选完成 / 取消、设日期（可选）、筛选（全部 / 未完成 / 已完成）、搜索。
- [ ] 番茄计时：25 / 5 循环、开始 / 暂停 / 跳过 / 重置、时长可调、阶段切换声音 + 桌面弹窗。
- [ ] 计时器（秒表）：开始 / 暂停 / 继续 / 计次 / 清零。
- [ ] 日历：月视图（含今天高亮）、上一月 / 下一月 / 回到今天、点日期看当天待办、日历内勾选完成。
- [ ] 待办与日历双向联动（同一数据源）。
- [ ] 本地数据持久化（关闭再打开数据不变）+ 文件损坏容错。
- [ ] 导航栏底部计时状态条（运行中显示当前时间与阶段，可跳转）。

---

## 8. 第一版暂时不做的功能

- 番茄钟绑定具体待办 / 统计「某任务花了几轮番茄」。
- 每日番茄 / 计时统计复盘（日历只排待办，不算账）。
- 倒计时（计时器只做秒表，不做倒计时）。
- 待办分类 / 标签 / 优先级 / 子任务（先保持简单）。
- 多设备同步、云备份、账号登录。
- Mac 版（先只做 Windows；需要再说）。
- 数据自动导出 / 导入备份（仅支持手动复制文件备份）。
- 待办「到期推送」等额外提醒（除番茄阶段切换的声音 + 弹窗外）。

---

## 9. 可验收标准（实测可检查）

1. **启动与离线**：Windows 双击启动，无需联网、无需登录；断网环境下全部功能可用；首屏为待办清单。
2. **新建待办**：填标题保存后列表立即出现该项；填了日期则在日历对应日显示数量圆点。
3. **完成 / 取消**：勾选后标题加删除线并可在「已完成」下看到；取消勾选恢复。
4. **编辑 / 删除**：可改标题 / 日期 / 备注；删除后从列表与日历同时消失。
5. **筛选 / 搜索**：全部 / 未完成 / 已完成切换结果正确；搜索按标题匹配。
6. **番茄计时**：默认 25/5，开始后倒计时；归零触发声音 + 弹窗并自动切入休息；暂停 / 跳过 / 重置均生效；调整时长后生效。
7. **秒表**：开始后正计时；计次记录每段时长；暂停 / 清零可用；清零后归零并清空计次。
8. **日历**：月视图日期正确（含今天高亮）；翻月正常；点某天右侧列出该日待办；日历内勾选完成与待办清单同步。
9. **持久化**：新增 / 完成 / 删除后关闭再打开程序，数据保持不变。
10. **备份恢复**：复制数据文件即备份；用备份覆盖后数据恢复。
11. **容错**：数据文件损坏时程序不崩溃，以空数据正常启动。
