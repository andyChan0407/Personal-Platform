# 开发计划 · 个人专属待办桌面应用（第一版）

> 依据：PRD.md（2026-09-14 确认）
> 目标：交付一个可在 Windows 双击运行的离线桌面程序，覆盖 PRD 第 7 节全部功能，逐条满足第 9 节验收标准。
> 形态：Windows 桌面程序，不登录、不联网、不连云，数据存本机文件。

---

## 0. 技术选型与关键决策

| 项 | 决策 | 理由 |
|---|---|---|
| 打包形态 | **Electron** 桌面程序（Windows，双击 exe 启动） | 真正桌面窗口、本地文件读写、离线；与 PRD「电脑桌面程序」一致 |
| 前端 | 原生 HTML / CSS / JS（不引框架） | 体积小、零运行时依赖、易维护、离线无网也能跑 |
| 数据存储 | Node `fs` 读写本地 JSON：`app.getPath('userData')/data.json` | 满足「单一本地文件 + 手动复制备份 + 损坏容错」 |
| 阶段提醒 | Web Audio 合成短提示音 + Electron `Notification` 弹窗 | 满足「声音 + 桌面弹窗」，无需外部音频文件 |
| 打包 | `electron-builder` 产出 Windows 可执行 | 一键生成双击即用的 exe |
| 备选方案 | 若介意体积，可改用 Tauri（需 Rust 环境）或纯网页 + 桌面快捷方式（但后者已被 PRD 排除） | 作为可调整项，不阻塞 |

> 说明：打包阶段需**联网下载一次** Electron 与依赖；程序本身运行全程离线。

---

## 1. 项目结构与初始化（Phase 1）

```
番茄计时器/
├─ package.json            # name/main/scripts(start,build)
├─ src/
│  ├─ main.js             # 主进程：建窗口、加载 index.html、注册 preload、Notification
│  ├─ preload.js          # contextBridge 暴露 storage / 设置 / 通知 API 给渲染进程
│  └─ renderer/
│     ├─ index.html       # 骨架：左侧 .sidebar + 右侧 .main
│     ├─ css/style.css    # 浅色主题（蓝 #185FA5 + 番茄红，扁平无阴影）
│     └─ js/
│        ├─ storage.js    # load/save JSON，损坏容错
│        ├─ store.js      # 待办增删改查 + 筛选/搜索/按日期聚合（纯函数）
│        ├─ bus.js        # 极简事件总线（模块间同步、状态条广播）
│        ├─ app.js        # 导航切换、底部计时状态条
│        ├─ todo.js       # 待办清单模块（首页）
│        ├─ pomodoro.js   # 番茄计时模块
│        ├─ stopwatch.js  # 计时器（秒表）模块
│        └─ calendar.js   # 日历模块
└─ assets/                # 预留（提示音优先用 Web Audio 合成，可不放文件）
```

**完成标志**：`npm start` 能打开窗口并显示空待办首页，控制台无报错。

---

## 2. 数据层：storage + store（Phase 2）

- Todo 结构（对齐 PRD 4.1）：`{ id, title, date, done, note, createdAt }`
- `settings`（并存于 data.json）：`{ workMin:25, breakMin:5 }`
- `storage.load()`：读 `data.json` → `JSON.parse` 包 `try/catch`；文件缺失/损坏 → 返回 `{ todos:[], settings:{workMin:25,breakMin:5} }`，**不崩溃**。
- `storage.save(data)`：先写临时文件再替换，避免半写损坏。
- `store` 纯函数：`addTodo / updateTodo / deleteTodo / toggleDone`；选择器：`filterByStatus / searchByTitle / groupByDate`。
- **完成标志**：用 Node 脚本自测增删改、损坏容错（对应验收 9、11）。

---

## 3. 整体框架与导航（Phase 3）

- `index.html` 骨架：`.sidebar`（标题 + 4 个 nav + 底部状态条槽位）、`.main`。
- `app.js`：点击导航切换 `.main` 内视图（只渲染当前模块），**默认显示待办清单**。
- 底部「计时状态条」：订阅 `bus` 的计时事件，运行中显示「番茄 mm:ss 工作中/休息中」或「秒表 mm:ss」，点击跳对应模块；无运行时不显示。
- 应用浅色主题样式。
- **完成标志**：4 模块切换正确、首页默认待办、状态条槽位就绪（验收 1、7 的部分）。

---

## 4. 待办清单模块（首页，Phase 4）

- 列表渲染：复选框 + 标题 + 日期徽标（有日期显示「M月D日」，无日期显示「未排期」）；完成项删除线 + 置灰。
- 筛选标签：全部 / 未完成 / 已完成（默认全部）。
- 搜索框：按标题实时匹配。
- 新建 / 编辑弹窗：标题必填校验、日期（`<input type=date>` 可选）、备注（可选）、保存 / 取消。
- 勾选完成 / 取消；删除（确认后移除）。
- 空状态引导文案。
- 任何变更后 `storage.save()` 并触发 `bus` 通知日历重算聚合。
- **完成标志**：覆盖验收 2（部分）、3、4、5。

---

## 5. 番茄计时模块（Phase 5）

- 状态：`{ phase:'work'|'break', remaining, completedCount, running }`。
- 开始：从 `workMin` 倒计时；归零 → 播放提示音 + `Notification`（「该休息了 / 该工作了」）→ 切阶段、重置 `remaining`、`work` 结束则 `completedCount+1`。
- 暂停 / 继续、跳过（直接切下一阶段）、重置（回到工作状态，`completedCount=0`）。
- 时长设置（默认 25 / 5）输入框，保存到 `settings` 并即时生效。
- `setInterval` 每秒 tick；运行中通过 `bus` 广播状态条。
- **完成标志**：验收 6。

---

## 6. 计时器（秒表）模块（Phase 6）

- 状态：`{ elapsedMs, running, laps:[] }`。
- 开始 / 暂停 / 继续；计次（push 当前累计段时长）；清零（归零并清空 `laps`）。
- 显示 `mm:ss.d`（分:秒.十分位）。
- 运行中通过 `bus` 广播状态条（显示「秒表 mm:ss」）。
- **完成标志**：验收 7。

---

## 7. 日历模块（Phase 7）

- 月视图：7 列（周一始），今天高亮；上一月 / 下一月 / 回到今天。
- 按 `todos.date` 聚合并联（同源，非独立存储）；有待办的日期格子显示数量圆点。
- 点击日期 → 右侧面板列出该日待办，可勾选完成（调 `toggleDone`，与清单同源同步）。
- **完成标志**：验收 2（日历圆点）、8，以及清单↔日历双向联动（验收 5 联动、PRD 第 5 节）。

---

## 8. 集成与联调（Phase 8）

- 统一 `bus`：store 变更后清单与日历重渲染（双向同步）。
- 计时状态条整合番茄 + 秒表（同一槽位，显示当前活跃计时器）。
- 边界核对：data 文件损坏启动空数据不崩；断网全功能可用；关闭重开数据保留。
- **完成标志**：验收 1、9、10、11 联调通过。

---

## 9. 打包为 Windows exe（Phase 9）

- 引入 `electron-builder`，配置 `build`（appId、productName、files、win target = nsis 或 portable）。
- `npm run build` 产出 `dist/*.exe`。
- 确认打包后数据文件路径仍为 `userData/data.json`（不被打包进只读资源）。
- **完成标志**：双击 exe 启动，全部功能可用。

---

## 10. 验收与交付（Phase 10）

- 逐条核对 PRD 第 9 节 11 项，产出「验收清单」并勾选。
- 交付物：`exe` 安装/便携包 + `PRD.md` + `开发计划.md` + 简短使用说明（含手动备份 `data.json` 指引）。
- 修复发现的问题，直至全部验收通过。

---

## 执行顺序与依赖

`0 选型 → 1 骨架 → 2 数据层 → 3 框架导航 → 4 待办 → 5 番茄 → 6 秒表 → 7 日历 → 8 集成联调 → 9 打包 → 10 验收`

- 4/5/6/7 在 Phase 3 后可**并行**推进（各自独立文件），但 Phase 8 集成需它们全部完成。
- Phase 2 的数据层自测可尽早做，提前锁定验收 9、11。

## 风险与备注

- **体积**：Electron 打包体积较大；若你介意，可改 Tauri（需 Rust）或纯网页 + 快捷方式（已被 PRD 排除），属可调整项。
- **环境限制**：当前构建环境为无界面 Windows，UI 最终需你在本地双击 exe 做可视化验收；数据层与计时逻辑我可用 Node 脚本自测。
- **提示音**：优先 Web Audio 合成短音，避免引入额外音频资源文件。
