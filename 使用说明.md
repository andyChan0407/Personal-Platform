# 使用说明 · 我的待办（个人专属桌面应用）

> 形态：Windows 桌面程序，离线可用，不登录、不联网、不连云，数据只在本机。

## 一、日常使用（最终用户）

1. **启动**：双击生成的 `我的待办.exe`（位于 `dist/` 目录）即可打开，无需安装、无需联网。
2. **桌面胶囊**：启动后桌面上出现的是一枚**药丸形胶囊**（默认贴屏幕右侧、始终置顶），不占用任务栏。
   - **左键单击胶囊** → 展开主功能界面；
   - **按住拖动胶囊** → 移动位置，松手后自动记住，下次启动回到原位；
   - **右键点击胶囊** → 中文菜单：打开主界面 / 隐藏胶囊 / 关于本应用 / 完全退出；
   - 胶囊上会实时显示番茄/秒表剩余时间（计时中胶囊变蓝并呼吸闪烁），空闲时显示「今日待办 N」。
   - **鼠标移入胶囊** → 胶囊下方展开「系统资源」面板，每秒刷新一次，显示：
     - **CPU**：当前使用率 + 核心数；
     - **内存**：共多少 G、已用多少 G、还剩多少 G、还剩百分之几可用；
     - **磁盘**：本机每个**固定硬盘**（C/D/E…）的共多少 G、还剩多少 G、还剩百分之几可用。
       剩余空间不足 20% 的磁盘变橙色、不足 10% 变红色，一眼看出哪块盘要满了。
   - 鼠标移开面板自动收起；点面板右上角「**钉住**」可固定常显（再点一次取消钉住）。
     胶囊贴到屏幕最底部时，面板会自动翻到胶囊**上方**显示；贴到最右侧时会向左展开，胶囊本身不会跳动。
   - 资源数据只在面板展开期间采集，收起即停，不会常驻占用 CPU；磁盘盘符清单缓存 1 分钟，避免反复起子进程。
   - **点主窗口右上角的 ×** → 只收起为胶囊，程序仍在后台运行，随时点胶囊再展开；
     **要真正退出进程，请用胶囊右键菜单的「完全退出」**（或菜单栏 文件 → 退出）。
3. **主题切换**：主界面**右上角**有三段式主题按钮（☀ 默认 / 🌙 深色 / 💧 毛玻璃），点一下就换，立即生效、无需重启。
   - **默认**：原来的浅色蓝白配色。
   - **深色**：整窗转深色，四个模块一起变；蓝色主色调亮一档，长时间使用不刺眼。
   - **毛玻璃**：窗内自带夜空壁纸（渐变 + 星点 + 月光晕），侧栏与卡片是半透明磨砂（背景模糊），文字走白灰阶。
   - 选择会保存到数据文件，**下次启动自动沿用上次的主题**；老数据文件没有该字段时按「默认」启动。
   - 胶囊（桌面那枚药丸）**不跟随主题**，保持橙色外观。
4. **菜单栏**：顶部菜单已全部中文化——文件 / 编辑 / 视图 / 窗口 / 帮助；「文件 → 打开数据目录」可直接定位到数据文件。
5. **首页**：打开即看到「待办清单」。点右上角「+ 新建待办」可填写标题（必填）、日期（可选）、备注（可选）。左侧「待办清单」旁的徽标会显示未完成数量。
6. **四个模块**（左侧导航切换，带图标）：
   - 待办清单：勾选完成、编辑、删除；按 全部 / 未完成 / 已完成 筛选；顶部搜索框按标题搜索。
   - 番茄计时：默认 25 分钟工作 + 5 分钟休息自动循环；圆形进度环显示当前阶段进度；可暂停 / 跳过 / 重置；底部可改时长；阶段切换有声音 + 桌面弹窗提醒。
   - 计时器：秒表，正计时，支持「计次」记录每段、清零。
   - 日历：月视图，点某天查看当天计划、可直接「+ 新增计划」（同一天支持多笔）并就地删除/勾选完成；有待办的日期显示数量圆点。
6. **计时状态条**：侧边栏底部在番茄或秒表运行时显示当前状态，点击可跳转对应模块。

## 二、数据存储与备份

- 所有数据保存在本机文件：`%APPDATA%/我的待办/data.json`（Electron 用户数据目录）。
- 胶囊的位置也记录在该文件的 `settings.capsulePos` 中，删掉该字段即回到默认位置。
- 主题选择记录在 `settings.theme`（`light` / `dark` / `glass`）；删掉该字段或写成非法值，都会按「默认」浅色主题启动。
- 主题与胶囊资源面板都是纯本机渲染，不联网、不上传。
- 胶囊上的 CPU / 内存 / 磁盘信息**只在本机内存中读取并显示**，不写入任何文件、不联网上传。
- **备份**：直接复制 `data.json` 到别处即可。
- **恢复**：用备份文件覆盖原 `data.json`，重启程序即恢复。
- **损坏保护**：若 `data.json` 损坏，程序会以空数据正常启动、不崩溃，下次写入会自动重建文件。

## 三、开发者：本地运行与打包

```bash
npm install              # 安装 electron 与 electron-builder（依赖国内镜像可提速）
npm start                # 开发模式启动（需桌面环境）
npm run pack:portable    # 离线打包：复用已下载的 Electron 运行时，产出 dist/我的待办.exe
npm run build            # 联网打包（electron-builder，需可访问 GitHub 资源；受限环境用上面的 pack:portable）
npm test                 # 运行全部阶段测试（test/all.js，含 jsdom 渲染层冒烟测试）
```

> 本机打包说明：因当前构建环境无法访问 GitHub 拉取 electron-builder 工具链，已改用 `npm run pack:portable` 离线方式——直接复用 `node_modules/electron/dist` 运行时，将 `src/` 挂到 `resources/app`，并把可执行文件重命名为 `我的待办.exe`。最终交付物即 `dist/我的待办.exe`，双击即用。

## 四、验收与已知限制

- 功能验收对照 `PRD.md` 第 9 节 11 条；自动化测试覆盖数据层、计时状态机、日历逻辑、持久化、容错，以及用 jsdom 加载全部渲染脚本的**集成冒烟测试**（启动→增删改→筛选→日历联动→番茄/秒表视图），见 `test/`。
- **当前环境为无界面服务器**，纯逻辑与渲染接线已由测试全量覆盖；UI 的“肉眼”可视化验收（窗口外观、声音/弹窗提醒）需在你本机双击 `dist/我的待办.exe` 完成。
- 第一版按计划未做：番茄绑定具体待办/统计、每日复盘、倒计时、标签优先级、多设备同步、Mac 版、自动导出导入、到期推送。
